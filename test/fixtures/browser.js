
const array = [ 1, 2, 3, 4 ].map(async number => {
    const string = "string";
    let suh = window.document.body.dataset.suh;

    console
        .log({ number,
            string });

    console.log(1, 2, 4);

    const element = {
        ah: Number.parseInt("3"),
        uh: async() => "uh"
    };
});

function y(lol = "eh", a) {
    alert("hey");
    throw new Error();

    hey(error);
}

document.body.addEventListener("scroll");
document.body.innerHTML = "<div>hey</div>";

y();
