
const path = require('/path/pateh')
import json from './index.js'

import a from '.'
let arr= [ 1, 2, 3, 4,].map(async(number )=>{
    var string =  "string"



    console.
        log ({number: number,
            string
    });

    console.log( 1, 2 ,
        4);

    let el = {
        'ah': parseInt('3'),
        uh: ()=> {
            return 'uh'
        }
    }
});

function y(lol = 'eh', a) {
    alert('hey')
    throw Error();

    hey(error)
}

y();

const fs = require('fs');
fs.exists('mypath');
